---
description: Build a ~4KB context bundle (graph + symbols + memory) for a task
argument-hint: [task description]
allowed-tools: Bash(icmg pack:*)
---
Build a focused context bundle for the task below, then use it to plan your work.

!`icmg pack "$ARGUMENTS"`
