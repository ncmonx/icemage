# Plan (managed by icmg)
<!-- icmg-slim: generated by `icmg plan import --slim`. Restore: `icmg plan restore --file .\CHANGELOG.md` -->

> Hooks inject relevant sections per-session (hot) and per-prompt (cold, BM25).
> Browse: `icmg plan list` | `icmg knowledge --html` | restore: `icmg plan restore`

## 1.3.0 — Skill power chain + Focus Chain + token cuts + bug-report

> Mega bundle: 15 features absorbed from Aider / Cline / Cursor / Repomix + own brainstorm. Skill subsystem becomes granular (per-pasal chunking, BM25+cosine hybrid recall via `icmg skill ask`, UserPromptSubmit auto-inject of top chunk above 0.20 threshold — kills grep-fallback when user names an ingested skill). Drift killed via Focus Chain (per-session todo persistence + SessionStart/UserPromptSubmit re-inject). Token cuts: `pack --compress-ast` regex-elides function bodies for C/C++/JS/TS/Py/Go/Rust (70-90% byte cut), `pack --show-tokens` before→after stats, new `icmg tokens` heuristic counter for files/stdin/dirs. Memory bank: `.icmgrules` git-tracked durable rules auto-loaded at init + SessionStart injects active rules, `.icmgignore` additive scanner exclude. Routing: `@symbol` resolver in pack/recall, PostToolUse:Bash `debug-context` auto-injects file paths + memory recall on FAIL/Error/Traceback signatures. History: `icmg ingest-history --pr-count N` ingests merged PRs as memoir nodes via `gh` CLI. Reporting: `icmg bug-report` collects diagnostics + files GH issue manually or batched (`--auto-capture` writes to crash-pending.jsonl, `--send-pending` batches via gh after explicit user consent — never auto-submits). Fixes #46: async version check on stale cache — hot path never blocks on GitHub API (2-18s latency spike eliminated); `ICMG_VERSION_CHECK_SYNC=1` forces foreground. Migrations 0028 skill_chunks, 0029 focus_chain, 0030 rules_bank — forward-only. Opt-outs per feature: `ICMG_SKILL_QUIET`, `ICMG_FOCUS_QUIET`, `ICMG_RULES_QUIET`, `ICMG_DEBUG_CONTEXT_QUIET`, `ICMG_SKIP_SERVICE`. 90/90 ctest pass Windows + Linux. Deferred to v1.3.1: Secretlint pack output scan, MCP lazy tool-schema, migration 0031 token_counts cache.

## 1.2.0 — Skill manifest + SessionStart auto-inject
> New `icmg skill manifest` subcommand emits a markdown block listing every indexed skill with its canonical `icmg context <node_key>` / `icmg recall` access pattern. The `CONTEXT_SESSION_SH` SessionStart hook now concatenates hot context_nodes + the manifest into one `additionalContext` payload — agents learn the routing for every ingested skill from turn 1, eliminating the grep-fallback regression on projects with skill-tier knowledge (KUHPerdata-style books, vendored docs, ADR sets). Manifest fails soft when the project DB is missing (silent no-op exit 0) so SessionStart can call it pre-`icmg init`. Test harness fix: `test_service_install_helper` migrated from gtest-style include to the project's in-tree TEST/ASSERT macros (mirrors the v1.1.1 inline patch). 72/72 ctest pass on Windows + Linux (+1 new test).

## 1.1.1 — Service auto-activate + legacy schtasks cleanup
> Patch release. Resident daemon `icmg service` (introduced v1.1.0) now installs **automatically** on every `icmg init`, `icmg update --apply`, and `icmg install --system` — single Windows logon-trigger task `icmg-service` replaces the legacy per-project autopilot schtasks. Legacy `icmg-backup-<hash>` / `icmg-maintain-<hash>` / `icmg-mirror-<hash>` / `icmg-sentinel-<hash>` / `icmg-shadow-upgrade*` entries are enumerated via `schtasks /Query /FO CSV /NH` and deleted on next init (the new `icmg-service` task itself is preserved). Opt-out via `ICMG_SKIP_SERVICE=1`. New helpers `core::installResidentService` + `core::cleanupLegacySchtasks` shared by init / install / service install paths. POSIX: helpers are no-op stubs (systemd/launchd unit creation out of scope). GitHub Sponsors badge dropped from README + `.github/FUNDING.yml`; Ko-fi kept as sole optional tip route. New test `test_service_install_helper`. 71/71 ctest pass on Windows + Linux (+1 new test).

## 1.1.0 — Perf baseline + enforcement + canonical popup fix
> First post-1.0 minor. Ten plan tasks shipped: `icmg bench` cmd (recall / compress / hooks / all subcmds + JSON), PreToolUse hard-deny (Claude Code hook v2 `permissionDecision: "deny"` for cat/head/tail/grep -r/powershell/cmd /c when icmg has equivalent; bypass `ICMG_STRICT_BYPASS=1`; audit-logs to `~/.icmg/strict-denials.jsonl`), caveman per-prompt re-inject (5 intensity tiers tied to 24h violation count; opt-out `ICMG_CAVEMAN_QUIET=1`), resident daemon `icmg service` (run/start/stop/status/install/uninstall — single logon-trigger replaces 5 per-project schtasks; in-process ticker calls Registry<BaseCommand> directly → zero subprocess fork → kills cmd/powershell popup at root), async Stop hook (sync toolBudgetReset + detached thread for heavy work), MemoryStore embed cache (warmEmbedCache bulk-loads all vecs on first recall; invalidated on store; chosen over HNSW because BM25 prefilter already caps candidates), diff-aware `recall --unseen` (migration 0027 `last_returned_session TEXT NULL`; stamps returned set so re-recall in same session returns less), `icmg skill-index` alias (regression fix). Stable contract from v1.0.0 preserved — all additions backward-compatible. 70/70 ctest pass on Windows + Linux (+4 new tests).

## 1.0.0 — Production-stable (Windows + Linux feature-complete)
> First v1.0 ceremony tag. Source-level parity reached across Win + Linux + macOS; production binaries ship for Windows x64 and Linux x64 (Linux built on WSL2 Ubuntu 24.04 / glibc 2.39). macOS arm64 source builds cleanly via the same cmake flow — prebuilt binary deferred to a self-hosted runner or paid CI path. **Stable contract from this tag:** wire formats (MCP tools, daemon RPC, hook JSON), SQLite schema (migrations forward-only), CLI surface, persistent paths. No more breaking changes on minor bumps. Added `tests/cli/test_hook_cmd.cpp` — CLI-layer contract test (registry + dispatch); combined with `test_hook_internals.cpp` since v0.57.0, the hook subsystem is fully covered. 66/66 ctest pass on Windows + Linux.

## 0.59.0 — Full feature parity Win + Linux + macOS arm64
> v0.58.0 caveats resolved. **Linux tree-sitter**: libtree-sitter runtime now vendored from source (v0.26.0) via CMake auto-fetch — apt's v0.20.x is no longer used; ABI matches `LANGUAGE_VERSION 15` declared by vendored grammars. Compiled as static `tree_sitter_runtime` from `lib/src/lib.c`. Downloaded with `curl -fsSLk` (MSYS2 cert chain unreliable for `file(DOWNLOAD)`). **macOS ONNX**: POSIX ORT extract switched from `file(ARCHIVE_EXTRACT)` + `file(COPY)` to shell `tar` + `cp -RP` — preserves `.dylib` symlink chain (`libonnxruntime.dylib → .1.dylib → .1.25.1.dylib`); macOS arm64 ONNX embedder links cleanly. Win bundle slimmed: `libtree-sitter-0.26.dll` / `wasmtime.dll` / `libzstd.dll` removed (vendored static tree-sitter has no transitive runtime deps); only `libwinpthread-1.dll` retained. apt + brew tree-sitter packages no longer needed in CI deps. 65/65 ctest pass on Windows. Multi-platform CI workflow disabled in this release for solo-dev cost reduction — file kept as `.disabled`; Win-only binary ships; Linux/macOS users build from source.

## 0.58.1 — Console popup hotfix
> `icmg.exe` was console-subsystem; Windows auto-allocated a console window for every headless invocation (Task Scheduler, Explorer) even with `-WindowStyle Hidden`. Fixed via two layers: (1) link with `-mwindows` so Windows never auto-creates a console for the process; `AttachConsole(ATTACH_PARENT_PROCESS)` at `main()` start + selective `freopen_s` of CONOUT$/CONIN$ preserves inherited shell pipes (bash/mintty redirects untouched) while binding to caller's console for cmd.exe interactive use. (2) Scheduler launcher: PowerShell `.ps1` → VBS via `wscript.exe //B //Nologo` — both wscript and the new GUI-subsystem icmg.exe are non-console, eliminating console allocation across the full scheduled-task chain. Users on existing installs: run `icmg init --force` per project to re-register tasks with the new VBS launcher. 65/65 ctest.

## 0.58.0 — Multi-platform CI scaffold (Phase 84 T2-T6)
> First cross-platform build: GitHub Actions matrix (`windows-latest`, `ubuntu-latest`, `macos-14 arm64`) builds + tests on every push; on tag push, packages per-OS archives with sha256 sidecars + publishes to GH Release. CMake conditional logic: ORT block per-OS archive naming/glob/RPATH; auto-download for SQLite amalgamation + nlohmann/json (gitignored sources). POSIX-portability fixes: macOS `_NSGetExecutablePath` via new `core::selfExePath()`; Apple ld64 `-force_load` instead of GNU `--whole-archive`; cross-platform `popen/pclose` guards in test_backup; `<climits>` for `INT_MAX`; `const fs::path&` for libc++ iterator; missing-`cmd` POSIX cron entries fixed in 5 auto-on commands. update_cmd `wantedAssetName()` now branches on `__aarch64__` / `__arm64__` for macOS arm64 asset naming. Feature parity caveats: Linux tree-sitter disabled (apt v0.20 ABI vs vendored v0.26 — v0.59.0 will vendor source); macOS ONNX disabled (dylib symlink extract — v0.59.0). 65/65 ctest pass on Windows; matrix CI proves Linux + macOS-arm64 green.

## 0.57.0 — Hook runners go fully in-process
> Phase B.B7 — eliminate the 4 inner subprocess forks per Stop event (`icmg distill auto` / `fail sync-denials` / `compliance check-thinking` / `tool-budget reset`) plus the `compress` fork in PostToolUse:Read. New `src/core/hooks/internals.{hpp,cpp}` exposes six standalone lib functions (`distillAuto`, `distillSession`, `complianceCheckThinking`, `failSyncDenials`, `toolBudgetReset`, `compressInPlace`) mirroring the corresponding CLI commands. `runStopHook` / `runPreCompactHook` / `runPostToolUseReadHook` now call them directly — net ~200-400ms saved per Stop event (4 × ~50-100ms cold-start each). Combined with v0.56.0 daemon RPC: hook event drops from "fork 5 icmg.exe" (~250-500ms) to "one socket round-trip + zero forks" (~5-10ms). CLI commands unchanged. 65/65 ctest pass (+11 new internals tests: compliance word counting, distill short-circuits, opt-out env vars, compress small-input guard).

## 0.56.0 — Daemon hook RPC + framing + auto-spawn fallback
> Phase B.B3-B6 — `icmg hook stop` / `hook precompact` / `hook posttool_read` now route through `rule-daemon` socket when available, eliminating icmg.exe outer fork (~30-50ms × event). Inline runner remains as automatic fallback. New `src/core/hooks/runners.{hpp,cpp}` exposes hook logic as standalone functions; both CLI handler and daemon dispatcher call them — single source of truth. Length-prefix framing (`Content-Length: N\r\n\r\n<body>`) added for responses ≥4KB so arbitrary-size compress payloads route correctly; smaller responses stay unframed (backward-compat with v0.55.x clients). Client `callHook(op, stdin, out_emit)` does one retry after `ensureDaemon()` if first attempt fails. 64/64 ctest pass; test_rule_daemon 25/25 (was 18 in v0.55.0). Plan: `docs/plans/2026-05-14-phase-b3-b6-plan.md`.

## 0.55.0 — Daemon dispatcher map + mutex + per-request workers
> Phase B.B2 — foundation for upcoming hook RPC ops. `src/daemon/rule_daemon.{hpp,cpp}` now routes opcodes through `std::unordered_map<string,Handler>` registered in ctor (O(1) dispatch, extensible without touching the eval loop). `std::mutex rules_mu_` guards all `rules_` reads/writes — checkFile uses snapshot-under-lock pattern; SET_STRICT / GET_STRICT atomic. POSIX server loop detaches `std::thread` per `accept()` so slow hook ops (B3-B6) won't starve concurrent rule-eval clients. User-visible behavior unchanged — wire format + return shapes identical. 64/64 ctest pass (+8 new daemon tests: dispatcher PING/RELOAD/SHUTDOWN/SET_STRICT roundtrip + malformed-JSON fallback + 16-thread concurrent SET_STRICT + 8-thread concurrent checkFile+toggle). B1 audit: `docs/plans/2026-05-14-phase-b1-daemon-audit.md`.

## 0.54.0 — Stop + PreCompact hooks in-process
> Two session-boundary events fold into single icmg invocation each: `icmg hook stop` replaces 5-bash-fork chain (distill auto / fail-sync / compliance check-thinking / tool-budget / wflog); `icmg hook precompact` folds snapshot + transcript distillation + ABSOLUTE-RULE re-injection (with top-5 pinned anchors). Outer bash + jq parsing layer per command eliminated. JSON output format unchanged — drop-in upgrade. Opt-out via ICMG_NO_STOP_HOOK=1 / ICMG_NO_PRECOMPACT_HOOK=1. PostToolUse:Read auto-compress intentionally left as shell flow because existing implementation does graph-context-first routing. 63/63 tests pass. Measured: ~150-300ms saved per session-boundary event.

## 0.53.3 — Build pipeline auto-tunes for speed
> CMake configure-time auto-detection probes for available build accelerators and wires them in transparently when safe. Opt-in flags expose the rest: `-DICMG_USE_LLD=ON` (lld linker, ~30-50% faster link on pure-MSYS without ONNX), `-DICMG_UNITY_BUILD=ON` (merge sources, ~20-40% faster compile). Dev iteration loop documented: `cmake --build build --target icmg` skips ~63 test executable link passes. Measured: cold full build ~7min (was ~20min); incremental main-binary build after single source touch: ~12s. 63/63 tests pass.

## 0.53.2 — Query layer warms under load
> Internal query layer caches prepared work-units across calls. Repeat queries within the same process now skip per-call prepare cost; identical query patterns finish noticeably faster on hot paths (recall, drift gate, path-context overlay). Bounded cache keeps memory predictable. Behaviour, return shapes, concurrency semantics all unchanged — drop-in upgrade. Fix: pre-existing Windows AV interference on temp-file rename in atomic-write test — now uses copy+delete fallback when scan-window blocks rename. 63/63 tests pass (+1 new prepared-statement regression test).

## 0.52.0 — Cross-session awareness + fail sync-denials + wake-up hook + routing docs
> `icmg session claim/clear/list` registers active tasks in `~/.icmg/active-work.json` for cross-session visibility. `icmg fail sync-denials` converts strict-denial logs to fail memory (auto-runs on Stop). `icmg init` now installs a SessionStart wake-up hook and auto-enables caveman ultra. AGENTS.md routing table expanded to 39 commands with topic-prefix convention. `PRAGMA mmap_size=256MB` added for read-heavy DB performance. 61/61 tests pass.

## 0.51.0 — Destructive-op guard + version staleness + security hardening
> `icmg run` intercepts destructive commands (rm -rf, DROP TABLE, git push --force) with `[y/N]` prompt; `--yes` bypasses. Version staleness check on startup (24h cached, lag≥10 soft-blocks init/graph). `icmg wake-up` filters by `created_by` for multi-user machines. Hook token efficiency: 4KB budget, BM25≥0.15, adaptive depth, 300s context cache, 1-hop BFS. CMD popup suppression on all Windows configs. `icmg init` warns on cloud-sync paths + sets owner-only permissions on `.icmg/`.

## 0.48.1 — Health schema check fix
> `icmg health` schema check now dynamically reads latest migration from `embeddedMigrations()` — no more hardcoded version drift. Reports `OK` when DB matches binary, `WARN` with actionable hint when mismatched.

## 0.48.0 — Command Gateway
> `icmg run` is the sole AI shell gateway — all agent shell commands route through it for audit log and blacklist enforcement. Leash hook (ID=50) enforces this mechanically via PreToolUse.

## 0.47.0 — C#/Ruby/Bash/Kotlin/Lua language support
> Tree-sitter extractors for C#, Ruby, Bash, Kotlin, Lua. Scheduled-task popup fix (no visible console on Win11). All 68 tests pass.

## 0.46.0 — Go/Rust/Java symbol extraction + backup restore-from
> Tree-sitter extractors for Go, Rust, Java (struct/fn/method/interface/enum/trait/class/constructor). `icmg graph update --lang go,rust,java` filters to specific languages. `icmg backup restore-from <file>` restores from any explicit .db path (cross-project / disaster recovery). All 63 tests pass.

## 0.45.1 — OpenSSF hardening
> node: `plan-0451-openssf-hardening` — `icmg knowledge get plan-0451-openssf-hardening`

## 0.45.0 — daemon IPC + rule-daemon
> node: `plan-0450-daemon-ipc-rule-daemon` — `icmg knowledge get plan-0450-daemon-ipc-rule-daemon`

## 0.44.0 — context-node match + BM25 skill injection
> node: `plan-0440-context-node-match-bm25` — `icmg knowledge get plan-0440-context-node-match-bm25`

## 0.43.1 — rule trial + supersession
> node: `plan-0431-rule-trial-supersession` — `icmg knowledge get plan-0431-rule-trial-supersession`

## 0.43.0 — rule enforcement daemon
> node: `plan-0430-rule-enforcement-daemon` — `icmg knowledge get plan-0430-rule-enforcement-daemon`

## 0.42.1 — rule trial (v0.42.1)
> node: `plan-0421-rule-trial` — `icmg knowledge get plan-0421-rule-trial`

## 0.42.0 — context graph: CLAUDE.md injection + skill index
> node: `plan-0420-context-graph` — `icmg knowledge get plan-0420-context-graph`

## 0.40.2 — system-wide install for shared-server deployments
> node: `plan-0402-system-wide-install-for-shared-server-deployments` — `icmg knowledge get plan-0402-system-wide-install-for-shared-server-deployments`

## 0.40.1 — zero-output-token file copy
> node: `plan-0401-zero-output-token-file-copy` — `icmg knowledge get plan-0401-zero-output-token-file-copy`

## 0.40.0 — plug-and-play setup
> node: `plan-0400-plug-and-play-setup` — `icmg knowledge get plan-0400-plug-and-play-setup`

## 0.39.3 — widened hook refresh detection
> node: `plan-0393-widened-hook-refresh-detection` — `icmg knowledge get plan-0393-widened-hook-refresh-detection`

## 0.37.2 — silent background operations on Windows
> node: `plan-0372-silent-background-operations-on-windows` — `icmg knowledge get plan-0372-silent-background-operations-on-windows`

## 0.37.1 — directory traversal hardening
> node: `plan-0371-directory-traversal-hardening` — `icmg knowledge get plan-0371-directory-traversal-hardening`

## 0.37.0 — fast path expansion + daemon scaffold
> node: `plan-0370-fast-path-expansion-daemon-scaffold` — `icmg knowledge get plan-0370-fast-path-expansion-daemon-scaffold`

## 0.36.0 — fast path: in-process prompt hook
> node: `plan-0360-fast-path-in-process-prompt-hook` — `icmg knowledge get plan-0360-fast-path-in-process-prompt-hook`

## 0.35.1 — auto-on bulletproof patch
> node: `plan-0351-auto-on-bulletproof-patch` — `icmg knowledge get plan-0351-auto-on-bulletproof-patch`

## 0.35.0 — autonomy stack: drift gate + sentinel watchdog + shadow auto-upgrade
> node: `plan-0350-autonomy-stack-drift-gate-sentinel-watchdog-shadow-auto-upgrade` — `icmg knowledge get plan-0350-autonomy-stack-drift-gate-sentinel-watchdog-shadow-auto-upgrade`

## 0.34.0 — self-protection: snapshot + dual-mirror + integrity gate + hot-cache
> node: `plan-0340-self-protection-snapshot-dual-mirror-integrity-gate-hot-cache` — `icmg knowledge get plan-0340-self-protection-snapshot-dual-mirror-integrity-gate-hot-cache`

## 0.33.6 — cross-project federation + autopilot hygiene + auto-zone
> node: `plan-0336-cross-project-federation-autopilot-hygiene-auto-zone` — `icmg knowledge get plan-0336-cross-project-federation-autopilot-hygiene-auto-zone`

## 0.33.5 — force memory/compress/store/graph active
> node: `plan-0335-force-memorycompressstoregraph-active` — `icmg knowledge get plan-0335-force-memorycompressstoregraph-active`

## 0.33.4 — coverage extensions
> node: `plan-0334-coverage-extensions` — `icmg knowledge get plan-0334-coverage-extensions`

## 0.33.3 — auto-scan + 7-layer dashboard + agent retry + 21 unit tests
> node: `plan-0333-auto-scan-7-layer-dashboard-agent-retry-21-unit-tests` — `icmg knowledge get plan-0333-auto-scan-7-layer-dashboard-agent-retry-21-unit-tests`

## 0.33.2 — caveman thinking enforcement + dashboard real-total
> node: `plan-0332-caveman-thinking-enforcement-dashboard-real-total` — `icmg knowledge get plan-0332-caveman-thinking-enforcement-dashboard-real-total`

## 0.33.1 — filter pipeline + context-budget + 7 features
> node: `plan-0331-filter-pipeline-context-budget-7-features` — `icmg knowledge get plan-0331-filter-pipeline-context-budget-7-features`

## 0.33.0 — Phase 67 batch (9 features + bug fix)
> node: `plan-0330-phase-67-batch-9-features-bug-fix` — `icmg knowledge get plan-0330-phase-67-batch-9-features-bug-fix`

## 0.32.10 — differential pack
> node: `plan-03210-differential-pack` — `icmg knowledge get plan-03210-differential-pack`

## 0.32.9 — anti-pattern + drift detector + diff-aware tests
> node: `plan-0329-anti-pattern-drift-detector-diff-aware-tests` — `icmg knowledge get plan-0329-anti-pattern-drift-detector-diff-aware-tests`

## 0.32.8 — discover + diff-summary perf
> node: `plan-0328-discover-diff-summary-perf` — `icmg knowledge get plan-0328-discover-diff-summary-perf`

## 0.32.7 — SP normalize + findSymbol guards
> node: `plan-0327-sp-normalize-findsymbol-guards` — `icmg knowledge get plan-0327-sp-normalize-findsymbol-guards`

## 0.32.6 — auto-think default + telemetry hygiene
> node: `plan-0326-auto-think-default-telemetry-hygiene` — `icmg knowledge get plan-0326-auto-think-default-telemetry-hygiene`

## 0.32.5 — JS/TS symbol JSON-array + scan opt-out
> node: `plan-0325-jsts-symbol-json-array-scan-opt-out` — `icmg knowledge get plan-0325-jsts-symbol-json-array-scan-opt-out`

## 0.32.4 — graph search + symbol resolution + fast incremental
> node: `plan-0324-graph-search-symbol-resolution-fast-incremental` — `icmg knowledge get plan-0324-graph-search-symbol-resolution-fast-incremental`

## 0.32.3 — path normalization + auto-migrate
> node: `plan-0323-path-normalization-auto-migrate` — `icmg knowledge get plan-0323-path-normalization-auto-migrate`

## 0.32.2 — strict-mode telemetry + savings integration
> node: `plan-0322-strict-mode-telemetry-savings-integration` — `icmg knowledge get plan-0322-strict-mode-telemetry-savings-integration`

## 0.32.1 — `icmg doctor`
> node: `plan-0321-icmg-doctor` — `icmg knowledge get plan-0321-icmg-doctor`

## 0.32.0 — strict mode WebFetch/Bash + DLL auto-rollback
> node: `plan-0320-strict-mode-webfetchbash-dll-auto-rollback` — `icmg knowledge get plan-0320-strict-mode-webfetchbash-dll-auto-rollback`

## 0.31.5 — MSYS routing fix
> node: `plan-0315-msys-routing-fix` — `icmg knowledge get plan-0315-msys-routing-fix`

## 0.31.4 — strict mode + Icemage brand mark
> node: `plan-0314-strict-mode-icemage-brand-mark` — `icmg knowledge get plan-0314-strict-mode-icemage-brand-mark`

## 0.31.3 — MCP icmg_compress telemetry fix
> node: `plan-0313-mcp-icmg-compress-telemetry-fix` — `icmg knowledge get plan-0313-mcp-icmg-compress-telemetry-fix`

## 0.31.2 — batch tests + Self-repair docs
> node: `plan-0312-batch-tests-self-repair-docs` — `icmg knowledge get plan-0312-batch-tests-self-repair-docs`

## 0.31.1 — per-DLL SHA256 + HTML reduce tests
> node: `plan-0311-per-dll-sha256-html-reduce-tests` — `icmg knowledge get plan-0311-per-dll-sha256-html-reduce-tests`

## 0.31.0 — health + telemetry prune + auto hook refresh
> node: `plan-0310-health-telemetry-prune-auto-hook-refresh` — `icmg knowledge get plan-0310-health-telemetry-prune-auto-hook-refresh`

## 0.30.x — savings daily chart + caveman directive
> node: `plan-030x-savings-daily-chart-caveman-directive` — `icmg knowledge get plan-030x-savings-daily-chart-caveman-directive`

## 0.29.x — sync + URL sanitize + 5 MCP tools + sha256 sidecars
> node: `plan-029x-sync-url-sanitize-5-mcp-tools-sha256-sidecars` — `icmg knowledge get plan-029x-sync-url-sanitize-5-mcp-tools-sha256-sidecars`

## Earlier
> node: `plan-earlier` — `icmg knowledge get plan-earlier`

