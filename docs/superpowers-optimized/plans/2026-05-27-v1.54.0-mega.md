# v1.54.0 mega Implementation Plan

> REQUIRED SUB-SKILL: superpowers-optimized:subagent-driven-development OR inline.

**Goal:** ship 4 features as v1.54.0 mega.
**Architecture:** see spec.
**Branch:** `release/v1.54.0` (forked from v1.53.0 HEAD `f4b9fd35c3`).
**Tech Stack:** C++17, MSVC Release, SQLite.

## Sub-A: NL skill remove fuzzy (Session 2)

### Task A0: Version bump 1.53.0 → 1.54.0
4 files via python script. Same pattern as prior bumps.

### Task A1: Verify v1.51 S3 unblocked
- Test: `tambah skill nl-test "regression test body"` via chat → check `~/.icmg/skills/nl-test.md` exists.
- Test: `delete skill nl-test` via chat → fuzzy resolves → file removed.
- If fail, debug fuzzy threshold or DB index.

### Task A2: Add NL skill CRUD integration test
Create `tests/cli/test_nl_skill_crud.cpp` — full cycle via icmg chat subprocess.

## Sub-B: Smart router B3 hook (Session 2)

### Task B1: Wire classifyPrompt into UserPromptSubmit
- Read `src/cli/commands/hook_cmd.cpp`. Find UserPromptSubmit handler.
- Call `icmg::cli::classifyPrompt(prompt)`. Emit `additionalContext: "[router-hint] LOCAL: short question"`.
- Honor `ICMG_FORCE_ROUTE` env override.

### Task B2: Integration test
Mock UserPromptSubmit input → assert additionalContext contains router-hint.

## Sub-C: ctest 32 baseline audit (Session 3)

### Task C1: Categorize failures
Run `ctest --output-on-failure` → group by error type:
- Registry: unknown key X → cmd not registered in test build
- DB schema mismatch → migration issue
- File path Windows-specific → path normalization bug

### Task C2: Fix or doc each category
Per-category branch + commit. Goal: pass ≥110/125.

## Sub-D: Track A Leiden + tree-sitter (Session 4-5)

### Task D1: Leiden algorithm skeleton
Create `src/graph/leiden.{hpp,cpp}`. Skeleton: refine() + move-nodes() phases. Reference: https://www.nature.com/articles/s41598-019-41695-z

### Task D2: Tree-sitter Go extractor
Vendor `tree-sitter-go` into `third_party/`. Create `src/graph/extractor/treesitter_go_extractor.cpp` mirroring existing Python extractor.

### Task D3: Rust extractor
Same pattern.

### Task D4: Java + C++ extractors
Same pattern.

### Task D5: Tests
6 unit tests covering each new language.

## Final ship task (Session 6)

- Full ctest sweep
- Pack zip + sha256
- Tag v1.54.0 + push private
- gh release create
- Docs PR (drop v1.49.0)
- state.md + session-log entry

## Local LLM policy (Rule #15)
When Claude response would be:
- <50 chars
- pure greeting/ack/yes-no
- no code/technical terms

THEN: optionally call `icmg llm respond <warmth-prompt>` and surface output.
ELSE: Claude direct.

Implementation: just behavioral; no code change. Persist as rule.
