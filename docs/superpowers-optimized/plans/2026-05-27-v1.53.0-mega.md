# v1.53.0 mega Implementation Plan

> REQUIRED SUB-SKILL: superpowers-optimized:subagent-driven-development or executing-plans.

**Goal:** ship 4 polish features as v1.53.0 mega bundle.
**Architecture:** see spec `docs/superpowers-optimized/specs/2026-05-27-v1.53.0-mega-design.md`.
**Branch:** `release/v1.53.0` (forked from `release/v1.52.0` HEAD `163ed34101`).
**Tech Stack:** C++17, MSVC Release, SQLite for dedup, std::async for parallel hooks.

---

## Sub-A: warm-loop polish (Session 1)

### Task A0: Version bump 1.52.0 → 1.53.0

**Files:** CMakeLists.txt, src/core/version.hpp, src/icmg.rc, src/icmg.exe.manifest.

- [ ] **Step 1:** Bump 4 files via python script (same pattern as prior bumps).
- [ ] **Step 2:** `grep "1\.(52|53)\.0"` — verify zero `1.52.0` strings, all `1.53.0`.
- [ ] **Step 3:** Commit `chore: bump version to 1.53.0`.

### Task A1: warm-loop active_clients counter RAII fix

**Files:** Modify `src/llm/warm_loop.cpp`.

- [ ] **Step 1:** Write failing test `tests/llm/test_warm_counter.cpp`:
```cpp
// Spawn 5 concurrent ping clients via PipeClient; verify daemon
// status served increments correctly and active_clients returns to 0.
// (Requires actual daemon — integration-style. May skip if pipe absent.)
```
Register in CMakeLists.

- [ ] **Step 2:** Replace manual `ds.active_clients++/--` with RAII guard in `warm_loop.cpp` worker:
```cpp
struct ClientCounter {
    DaemonState& ds_;
    explicit ClientCounter(DaemonState& ds) : ds_(ds) { ds_.active_clients++; }
    ~ClientCounter() { ds_.active_clients--; }
};
// In worker:
{
    ClientCounter cc(ds);
    auto req = server.readMessage(**conn);
    ...
}
server.disconnect(**conn);  // outside guard
```

- [ ] **Step 3:** Build icmg_lib + test.
- [ ] **Step 4:** Commit `fix(v1.53): warm-loop active_clients RAII guard`.

### Task A2: daemon-aware skip in-process WarmPool

**Files:** Modify `src/cli/commands/chat_cmd.cpp`, `src/cli/commands/llm_cmd.cpp` cmdRespond.

**Does NOT cover:** Hook layer (CLI only).

- [ ] **Step 1:** In chat_cmd warm probe path, capture `warmAvailable()` result before probe.
- [ ] **Step 2:** If `warm_was_available && !warm_hit`, skip `WarmPool::acquire`, print `(daemon owns model, warm path failed — falling to agent)` and `continue;` to next REPL turn.
- [ ] **Step 3:** Same logic in `llm_cmd.cpp::cmdRespond`.
- [ ] **Step 4:** Build Release + smoke: start daemon → chat → verify no "warm-pool acquire: load failed: RAM guard refuse" noise.
- [ ] **Step 5:** Commit `fix(v1.53): skip in-process acquire when daemon owns model`.

---

## Sub-B: smart router (Session 2)

### Task B1: router skeleton + 6 unit tests

**Files:**
- Create: `src/cli/router.hpp`, `src/cli/router.cpp`, `tests/cli/test_router.cpp`.

- [ ] **Step 1:** Write 6 failing tests (route classification).
- [ ] **Step 2:** Build → fail.
- [ ] **Step 3:** Impl `classifyPrompt` with rule precedence cache → LOCAL → CLOUD → default.
- [ ] **Step 4:** Build → pass.
- [ ] **Step 5:** Commit.

### Task B2: hash cache for CACHE route

**Files:** Modify `src/cli/router.cpp` add SQLite-backed dedup cache.

- [ ] Reuse dedup_cache impl from Sub-C1.

### Task B3: hook integration

**Files:** Modify `src/cli/hook_chain.cpp` or `src/cli/commands/userprompt_cmd.cpp` (whichever handles UserPromptSubmit).

- [ ] Emit `additionalContext: "[router-hint] LOCAL/CLOUD/CACHE: <reason>"` on each prompt submit.
- [ ] Test: 1 integration.

---

## Sub-C: dedup + parallel hooks (Session 3)

### Task C1: dedup_cache

**Files:**
- Create: `src/cli/dedup_cache.hpp`, `src/cli/dedup_cache.cpp`, `tests/cli/test_dedup_cache.cpp`.
- Create: `migrations/global/0033_prompt_dedup.sql`.

- [ ] 4 unit tests (insert+hit, miss-expired, miss-different, evict).
- [ ] FNV-128 hash impl (or reuse existing in `src/core/`).
- [ ] SQLite CRUD: insert/lookup/purge.
- [ ] Wire into PreToolUse:Bash hook handler.

### Task C3: hook parallel

**Files:**
- Create: `src/core/hook_parallel.cpp`.
- Modify: `src/cli/hook_chain.cpp`.

- [ ] 3 unit tests (2 indep parallel, blocking, opt-out).
- [ ] Read hooks.json — detect `"parallel": true` flag per hook.
- [ ] Run independent hooks via `std::async`, join + aggregate.
- [ ] Block if any.

---

## Sub-D: NL ambiguity (Session 2 or 3, smallest)

### Task D1: handleNL pending stash

**Files:** Modify `src/cli/auto_rule.hpp` + `src/cli/commands/chat_cmd.cpp`.

- [ ] Add `NLAction::PICK_PENDING` enum.
- [ ] `handleNL` ambiguous path stores candidates in static thread_local OR caller-provided context.
- [ ] Chat REPL stores `pending_disambig: std::optional<...>` + checks on each turn.
- [ ] On numeric input matching pending range → resolve + execute original action.
- [ ] Else → clear pending, normal flow.

### Task D2: 3 unit tests

**Files:** Modify `tests/cli/test_auto_rule.cpp`.

- [ ] test_ambiguous_stash: detectNL returns ambiguous + stash filled.
- [ ] test_pick_valid: pending + "2" input → resolves to candidate[1].
- [ ] test_clear_on_other: pending + non-numeric input → cleared, normal action.

### Task D3: commit + ship together with C tasks

---

## Final ship task (end of session 3)

- [ ] Full ctest sweep.
- [ ] Pack zip + sha256 (Win-only).
- [ ] Tag v1.53.0, push private branch+tag.
- [ ] gh release create.
- [ ] Docs PR (drop v1.48.0, add v1.53.0).
- [ ] state.md + session-log entry.
