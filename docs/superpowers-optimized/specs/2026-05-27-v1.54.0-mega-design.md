# v1.54.0 mega — NL skill fuzzy + smart router hook + ctest audit + Track A

**Status:** draft 2026-05-27
**Branch:** `release/v1.54.0` (forked from `release/v1.53.0` HEAD `f4b9fd35c3`)
**Ship policy:** Win-only mega, multi-session exec.

## Sub-features

### A. NL skill remove fuzzy unblock (~1h, priority 1)
- v1.51 Sub-D delete-skill NL path was blocked by Unicode skill_index crash.
- v1.53 scanner Unicode fix unblocks the path.
- Verify: `tambah skill X "body"` → indexes → `delete skill X` via fuzzy → removes.
- Add 1 integration test for full NL skill CRUD cycle.

### B. Smart router B3 hook integration (~2h)
- Wire v1.53 `classifyPrompt` into UserPromptSubmit hook (`hook_cmd.cpp`).
- Emit additionalContext: `[router-hint] route=LOCAL/CLOUD/CACHE reason=<rule>`.
- Allow user override via `ICMG_FORCE_ROUTE=LOCAL|CLOUD` env.
- 1 integration test.

### C. ctest 32 baseline fails audit (~3-5h)
- Categorize 32 failures by root cause (focus_cmd, skill_chunk, target_verify, etc).
- Fix or document non-goal per category.
- Goal: get baseline to ≥110/125 pass (90%+).

### D. Track A Leiden + tree-sitter expand (~8-10h, BIG)
- D1: Leiden clustering algorithm for graph community detection.
- D2: Add tree-sitter Go/Rust/Java/C++ extractors (currently Python/Ruby/Bash/Lua only).
- D3: Symbol extraction wired through new extractors.
- D4: 4-6 unit tests.

## Local LLM usage policy (Rule #15 proposal)
- Trivial chat replies (Indonesian small-talk, acks, "siap", "ok") → may route via `icmg llm respond`.
- Code/plan/spec/debug → Claude direct (faster, on-topic).
- Decision criteria: response <50 chars + pure-greeting/ack → consider local; else Claude.

## Non-goals
- Linux warm-loop unix-socket impl (defer indefinitely)
- ToolCallCache redesign (already works)
- C3 hook-chain parallel (harness owns scheduling, infeasible)

## Rollout (multi-session)

**Session 1 (this):** Spec + plan + branch created. No impl.
**Session 2:** Sub-A + Sub-B (small, ~3h combined).
**Session 3:** Sub-C ctest audit (~5h).
**Session 4-5:** Sub-D Leiden + tree-sitter (BIG, ~10h).
**Session 6:** Ship.

## Adversarial failure modes

1. **Sub-D Leiden scope creep**: Algorithm needs careful tuning + performance benchmarks. Mitigation: scope Leiden to bounded graph sizes first; iterative impl.
2. **Sub-B router rules misfire**: Indonesian prompts misclassified due to English-biased keywords. Mitigation: extend ID keyword list aggressively + telemetry.
3. **Sub-C ctest fixes risk regression**: Touching old tests may break other things. Mitigation: per-fix branch + smoke-test before merge.
